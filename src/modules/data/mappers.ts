import {
    Material, User, MaterialCategory, StockMovement, AttendanceLog, MaterialRequest, PurchaseRequest,
    ReturnRequest, PurchaseLot, PurchaseOrder, Supplier, SupplierPayment, SalaryAdvance, QuoteRequest, GoodsReceipt, CostCenter,
    AssignedSafetyTask, ChecklistTemplate, SafetyInspection, BehaviorObservation,
    WorkItem, ProgressLog, PaymentState, DailyTalk, MaintenanceOrder, MaintenanceLog, EADocument,
    ProtocolTemplate, Protocol, ShiftSchedule, Client, Contract, ContractWorker,
    Warehouse, WarehouseContract, MaterialStock,
    RentalParty, RentalContract, RentalAsset, RentalPayment, WorkReport,
    RentalRequest, RentalQuoteRequest,
    LeaveRequest, HRDocument, WorkOrder, WorkWeeklyReport
} from '@/modules/core/lib/data';
import { createDefaultHousekeeping } from '@/modules/core/lib/work-report-housekeeping';

export const mappers = {
    materials: (item: any): Material => ({
        id: item.id,
        name: item.name,
        stock: Number(item.stock),
        inUse: Number(item.in_use),
        minStock: item.min_stock != null ? Number(item.min_stock) : undefined,
        unit: item.unit,
        category: item.category,
        supplierId: item.supplier_id,
        archived: item.archived,
        class: item.criticality,
        usageType: item.usage_type,
        description: item.description,
        unitCost: Number(item.unit_cost),
        acquisitionDate: item.acquisition_date,
        serialNumber: item.serial_number,
        status: item.status,
        photos: item.photos,
        requiresMaintenance: item.requires_maintenance ?? false,
        nextMaintenanceDate: item.next_maintenance_date,
        isITAsset: item.is_it_asset,
        internalCode: item.internal_code,
        location: item.location,
        brand: item.brand,
        technicalSheetUrl: item.technical_sheet_url,
        technicalSheetName: item.technical_sheet_name,
        conditionScore: item.condition_score,
        failureProbability: item.failure_probability || 1,
        failureImpact: item.failure_impact || 1,
        parentId: item.parent_id || null,
        ownership: item.ownership || 'propio',
        rentalContractId: item.rental_contract_id || null,
        rentalAssetId: item.rental_asset_id || null,
        clientId: item.client_id || null,
    }),
    profiles: (item: any): User => ({
        id: item.id,
        name: item.name,
        email: item.email,
        role: item.role,
        qrCode: item.qr_code,
        tenantId: item.tenant_id,
        rut: item.rut,
        internalId: item.internal_id,
        cargo: item.cargo,
        phone: item.phone,
        fechaIngreso: item.fecha_ingreso ? new Date(item.fecha_ingreso) : null,
        baseSalary: item.base_salary,
        afp: item.afp,
        tipoSalud: item.tipo_salud,
        cargasFamiliares: item.cargas_familiares,
        signature: item.signature,
        biometric_template: item.biometric_template || null,
        // kyc_id_front/back/face_image se movieron a la tabla protegida
        // profile_documents (S4). Se cargan bajo demanda, no en el collection.
        enrolledBy: item.enrolled_by || null,
        enrolledAt: item.enrolled_at || null,
        onboardingCompleted: item.onboarding_completed || false,
        grantedPermissions: item.granted_permissions || [],
        address: item.address || undefined,
        birthDate: item.birth_date ? new Date(item.birth_date) : null,
        emergencyContactName: item.emergency_contact_name || undefined,
        emergencyContactPhone: item.emergency_contact_phone || undefined,
        employmentStatus: item.employment_status || 'active',
    }),
    material_categories: (item: any): MaterialCategory => ({
        id: item.id,
        name: item.name,
        parentId: item.parent_id ?? null,
    }),
    stock_movements: (item: any): StockMovement => ({
        id: item.id,
        materialId: item.material_id,
        materialName: item.material_name || '',
        quantityChange: item.quantity_change || item.quantity,
        newStock: item.new_stock || 0,
        type: item.type,
        date: item.date ? new Date(item.date) : new Date(),
        justification: item.justification || item.reason || '',
        userId: item.user_id || item.worker_id || '',
        userName: item.user_name || '',
        relatedRequestId: item.related_request_id || undefined,
        contractId: item.contract_id || null,
        contractName: item.contract_name || null,
        warehouseId: item.warehouse_id || null,
    }),
    attendance_logs: (item: any): AttendanceLog => ({
        id: item.id,
        userId: item.user_id,
        userName: item.user_name || '',
        timestamp: item.timestamp ? new Date(item.timestamp) : new Date(),
        type: item.type,
        method: item.method || 'qr',
        registrarId: item.registrar_id || '',
        registrarName: item.registrar_name || '',
        date: item.date || (item.timestamp ? item.timestamp.split('T')[0] : ''),
        contractId: item.contract_id || null,
        originalTimestamp: item.original_timestamp ? new Date(item.original_timestamp) : null,
        modifiedAt: item.modified_at ? new Date(item.modified_at) : null,
        modifiedBy: item.modified_by || null,
    }),
    shift_schedules: (item: any): ShiftSchedule => ({
        id: item.id,
        tenantId: item.tenant_id,
        name: item.name,
        shiftType: item.shift_type,
        daysOn: Number(item.days_on),
        daysOff: Number(item.days_off),
        // Postgres `time` llega como "HH:MM:SS"; los cálculos parsean "HH:mm".
        workStart: (item.work_start || '').slice(0, 5),
        workEnd: (item.work_end || '').slice(0, 5),
        isNightShift: item.is_night_shift || false,
        lunchStart: item.lunch_start ? String(item.lunch_start).slice(0, 5) : undefined,
        lunchEnd: item.lunch_end ? String(item.lunch_end).slice(0, 5) : undefined,
        rotationReferenceDate: item.rotation_reference_date,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
    }),
    clients: (item: any): Client => ({
        id: item.id,
        tenantId: item.tenant_id,
        name: item.name,
        rut: item.rut || undefined,
        contactName: item.contact_name || undefined,
        contactEmail: item.contact_email || undefined,
        contactPhone: item.contact_phone || undefined,
        notes: item.notes || undefined,
        status: item.status || 'active',
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
    }),
    contracts: (item: any): Contract => ({
        id: item.id,
        tenantId: item.tenant_id,
        name: item.name,
        code: item.code || undefined,
        // Tenants cuya BD aún no corrió la migración de áreas internas devuelven
        // undefined: se asume 'client', que es como se comportaba todo hasta ahora.
        kind: item.kind === 'internal' ? 'internal' : 'client',
        clientId: item.client_id || null,
        clientName: item.client_name || undefined,
        location: item.location || undefined,
        status: item.status,
        startDate: item.start_date,
        endDate: item.end_date || null,
        description: item.description || undefined,
        costCenterId: item.cost_center_id || null,
        createdBy: item.created_by || undefined,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        isSubcontractor: item.is_subcontractor ?? false,
        parentContractId: item.parent_contract_id || null,
        subcontractorCompany: item.subcontractor_company || null,
        subcontractorRut: item.subcontractor_rut || null,
    }),
    contract_workers: (item: any): ContractWorker => ({
        id: item.id,
        tenantId: item.tenant_id,
        contractId: item.contract_id,
        userId: item.user_id,
        shiftScheduleId: item.shift_schedule_id || null,
        roleInContract: item.role_in_contract || undefined,
        startDate: item.start_date || null,
        endDate: item.end_date || null,
        rotationStartDate: item.rotation_start_date || null,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
    }),
    warehouses: (item: any): Warehouse => ({
        id: item.id,
        tenantId: item.tenant_id,
        name: item.name,
        location: item.location || null,
        managerId: item.manager_id || null,
        managerName: item.manager_name || null,
        status: item.status || 'active',
        notes: item.notes || null,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
    }),
    warehouse_contracts: (item: any): WarehouseContract => ({
        id: item.id,
        tenantId: item.tenant_id,
        warehouseId: item.warehouse_id,
        contractId: item.contract_id,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
    }),
    material_stocks: (item: any): MaterialStock => ({
        id: item.id,
        tenantId: item.tenant_id,
        materialId: item.material_id,
        contractId: item.contract_id || null,
        warehouseId: item.warehouse_id || null,
        qty: Number(item.qty) || 0,
    }),
    rental_parties: (item: any): RentalParty => ({
        id: item.id,
        tenantId: item.tenant_id,
        name: item.name,
        partyType: item.party_type,
        rut: item.rut || undefined,
        contactName: item.contact_name || undefined,
        email: item.email || undefined,
        phone: item.phone || undefined,
        address: item.address || undefined,
        bank: item.bank || undefined,
        accountType: item.account_type || undefined,
        accountNumber: item.account_number || undefined,
        notes: item.notes || undefined,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
    }),
    rental_contracts: (item: any): RentalContract => ({
        id: item.id,
        tenantId: item.tenant_id,
        code: item.code || undefined,
        direction: item.direction,
        partyId: item.party_id,
        title: item.title,
        status: item.status,
        startDate: item.start_date,
        endDate: item.end_date || null,
        billingCycle: item.billing_cycle,
        amount: Number(item.amount) || 0,
        currency: item.currency || 'CLP',
        paymentDay: item.payment_day ?? null,
        taxRate: item.tax_rate != null ? Number(item.tax_rate) : 19,
        notes: item.notes || undefined,
        ocNumber: item.oc_number || undefined,
        ocStatus: item.oc_status || 'confirmed',
        ocSentAt: item.oc_sent_at || null,
        ocConfirmedAt: item.oc_confirmed_at || null,
        paymentTermsDays: item.payment_terms_days != null ? Number(item.payment_terms_days) : 30,
        clientContractId: item.client_contract_id || null,
        createdBy: item.created_by || undefined,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
    }),
    rental_assets: (item: any): RentalAsset => ({
        id: item.id,
        tenantId: item.tenant_id,
        contractId: item.contract_id,
        name: item.name,
        category: item.category,
        identifier: item.identifier || undefined,
        quantity: Number(item.quantity) || 1,
        unitPrice: item.unit_price != null ? Number(item.unit_price) : undefined,
        startDate: item.start_date || null,
        endDate: item.end_date || null,
        status: item.status || 'active',
        notes: item.notes || undefined,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
    }),
    rental_payments: (item: any): RentalPayment => ({
        id: item.id,
        tenantId: item.tenant_id,
        contractId: item.contract_id,
        dueDate: item.due_date,
        amount: Number(item.amount) || 0,
        status: item.status,
        paidDate: item.paid_date || null,
        paymentMethod: item.payment_method || undefined,
        reference: item.reference || undefined,
        notes: item.notes || undefined,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
    }),
    rental_requests: (item: any): RentalRequest => {
      const rawItems = Array.isArray(item.items) ? item.items : [];
      const items = rawItems.length
        ? rawItems.map((it: any) => ({
            name: it.name,
            category: it.category || 'other',
            quantity: Number(it.quantity) || 1,
          }))
        // Fallback: filas legacy mono-ítem sin columna items[].
        : [{ name: item.equipment_name, category: item.category || 'other', quantity: Number(item.quantity) || 1 }];
      return {
        id: item.id,
        tenantId: item.tenant_id,
        internalCode: item.internal_code || undefined,
        items,
        equipmentName: item.equipment_name || items[0]?.name,
        category: item.category || items[0]?.category,
        quantity: Number(item.quantity) || items[0]?.quantity || 1,
        startDate: item.start_date || null,
        endDate: item.end_date || null,
        billingCycleEstimate: item.billing_cycle_estimate || 'monthly',
        contractId: item.contract_id || null,
        contractName: item.contract_name || null,
        area: item.area || undefined,
        justification: item.justification || undefined,
        supervisorId: item.supervisor_id,
        supervisorName: item.supervisor_name || undefined,
        status: item.status,
        approverId: item.approver_id || undefined,
        approverName: item.approver_name || undefined,
        approvalDate: item.approval_date ? new Date(item.approval_date) : undefined,
        rejectionDate: item.rejection_date ? new Date(item.rejection_date) : undefined,
        rejectionReason: item.rejection_reason || undefined,
        rentalContractId: item.rental_contract_id || null,
        notes: item.notes || undefined,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        adcAuthorizedAt: item.adc_authorized_at || null,
        adcAuthorizedById: item.adc_authorized_by || null,
      };
    },
    rental_quote_requests: (item: any): RentalQuoteRequest => ({
        id: item.id,
        tenantId: item.tenant_id,
        internalCode: item.internal_code || undefined,
        title: item.title || '',
        status: item.status,
        requestIds: item.request_ids || [],
        items: item.items || [],
        partyIds: item.party_ids || [],
        responses: item.responses || [],
        deadline: item.deadline || undefined,
        notes: item.notes || undefined,
        awardedPartyId: item.awarded_party_id || undefined,
        awardedQuoteId: item.awarded_quote_id || undefined,
        awardedAt: item.awarded_at || undefined,
        rentalContractId: item.rental_contract_id || undefined,
        createdBy: item.created_by || undefined,
        createdByName: item.created_by_name || '',
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        updatedAt: item.updated_at ? new Date(item.updated_at) : undefined,
    }),
    work_reports: (item: any): WorkReport => ({
        id: item.id,
        tenantId: item.tenant_id,
        internalCode: item.internal_code,
        status: item.status,
        workItemId: item.work_item_id || null,
        otNumber: item.ot_number || '',
        client: item.client || '',
        faena: item.faena || '',
        area: item.area || '',
        supervisorId: item.supervisor_id || '',
        supervisorName: item.supervisor_name || '',
        workDate: item.work_date,
        startTime: item.start_time || null,
        endTime: item.end_time || null,
        location: item.location || null,
        obra: item.obra || null,
        contractNumber: item.contract_number || null,
        addendumNumber: item.addendum_number || null,
        shift: item.shift || null,
        specialty: item.specialty || null,
        emittedBy: item.emitted_by || null,
        emittedByRole: item.emitted_by_role || null,
        workSchedule: item.work_schedule || null,
        dayNight: item.day_night || null,
        lunchStart: item.lunch_start || null,
        restartTime: item.restart_time || null,
        activities: item.activities || '',
        structuredActivities: item.structured_activities || [],
        dailyOts: item.daily_ots || [],
        consolidatedOrderIds: item.consolidated_order_ids || [],
        consolidatedOrdersSnapshot: item.consolidated_orders_snapshot || null,
        // Coerción de formato legacy: `hours` antes era número escalar; ahora es
        // un objeto {otId: horas}. Normaliza para que la UI/PDF no rompan.
        labor: (item.labor || []).map((l: any) => ({
            ...l,
            hours: l.hours && typeof l.hours === 'object' ? l.hours : {},
        })),
        equipment: item.equipment || [],
        interferences: item.interferences || [],
        materials: item.materials || [],
        nextDayPlan: item.next_day_plan || [],
        housekeeping: item.housekeeping || createDefaultHousekeeping(),
        photos: item.photos || [],
        progressPercent: Number(item.progress_percent) || 0,
        executionStatus: item.execution_status || 'not_started',
        progressObservations: item.progress_observations || null,
        progressHistory: item.progress_history || [],
        signatures: item.signatures || [],
        auditLog: item.audit_log || [],
        rejectionReason: item.rejection_reason || null,
        sentTo: item.sent_to || [],
        createdBy: item.created_by || '',
        createdByName: item.created_by_name || '',
        updatedBy: item.updated_by || null,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        updatedAt: item.updated_at ? new Date(item.updated_at) : new Date(),
        submittedAt: item.submitted_at ? new Date(item.submitted_at) : null,
        operationsApprovedAt: item.operations_approved_at ? new Date(item.operations_approved_at) : null,
        finalApprovedAt: item.final_approved_at ? new Date(item.final_approved_at) : null,
    }),
    work_orders: (item: any): WorkOrder => ({
        id: item.id,
        tenantId: item.tenant_id,
        otNumber: item.ot_number || '',
        otNumberSource: item.ot_number_source || 'manual',
        client: item.client || '',
        contractNumber: item.contract_number || null,
        area: item.area || null,
        location: item.location || null,
        specialty: item.specialty || null,
        milestone: item.milestone || null,
        supervisorId: item.supervisor_id || null,
        supervisorName: item.supervisor_name || '',
        shift: item.shift || null,
        workSchedule: item.work_schedule || null,
        workDate: item.work_date,
        description: item.description || '',
        labor: item.labor || [],
        equipment: item.equipment || [],
        materials: item.materials || [],
        photos: item.photos || [],
        plannedPercent: Number(item.planned_percent) || 0,
        executedPercent: Number(item.executed_percent) || 0,
        status: item.status || 'draft',
        createdBy: item.created_by || null,
        createdByName: item.created_by_name || null,
        updatedBy: item.updated_by || null,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        updatedAt: item.updated_at ? new Date(item.updated_at) : new Date(),
    }),
    work_weekly_reports: (item: any): WorkWeeklyReport => ({
        id: item.id,
        tenantId: item.tenant_id,
        title: item.title || '',
        client: item.client || '',
        faena: item.faena || '',
        obra: item.obra || null,
        contractNumber: item.contract_number || null,
        area: item.area || null,
        specialty: item.specialty || null,
        supervisorId: item.supervisor_id || null,
        supervisorName: item.supervisor_name || '',
        startDate: item.start_date,
        endDate: item.end_date,
        consolidatedReportIds: item.consolidated_report_ids || [],
        consolidatedReportsSnapshot: item.consolidated_reports_snapshot || null,
        observations: item.observations || null,
        shiftHandover: item.shift_handover || null,
        signatures: item.signatures || [],
        status: item.status || 'draft',
        createdBy: item.created_by || null,
        createdByName: item.created_by_name || null,
        updatedBy: item.updated_by || null,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        updatedAt: item.updated_at ? new Date(item.updated_at) : new Date(),
    }),
    hr_leave_requests: (item: any): LeaveRequest => ({
        id: item.id,
        tenantId: item.tenant_id,
        userId: item.user_id,
        userName: item.user_name || '',
        type: item.type,
        startDate: item.start_date,
        endDate: item.end_date,
        daysCount: Number(item.days_count) || 1,
        reason: item.reason || null,
        status: item.status,
        supportingDocumentUrl: item.supporting_document_url || null,
        reviewedBy: item.reviewed_by || null,
        reviewedByName: item.reviewed_by_name || null,
        reviewedAt: item.reviewed_at ? new Date(item.reviewed_at) : null,
        rejectionReason: item.rejection_reason || null,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
    }),
    hr_documents: (item: any): HRDocument => ({
        id: item.id,
        tenantId: item.tenant_id,
        userId: item.user_id,
        userName: item.user_name || '',
        documentType: item.document_type,
        name: item.name,
        fileUrl: item.file_url || null,
        filePath: item.file_path || null,
        issueDate: item.issue_date || null,
        expiryDate: item.expiry_date || null,
        notes: item.notes || null,
        createdBy: item.created_by || null,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
    }),
    material_requests: (item: any): MaterialRequest => ({
        id: item.id,
        internalCode: item.internal_code,
        items: item.items,
        area: item.area,
        contractId: item.contract_id,
        contractName: item.contract_name,
        supervisorId: item.supervisor_id,
        status: item.status,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        userName: item.user_name || item.supervisor_name,
        approvalDate: item.approval_date ? new Date(item.approval_date) : undefined,
        rejectionDate: item.rejection_date ? new Date(item.rejection_date) : undefined,
        deliveryDate: item.delivery_date ? new Date(item.delivery_date) : undefined,
        approverId: item.approver_id,
        approverName: item.approver_name,
        notes: item.notes,
        tenantId: item.tenant_id,
        highestClass: item.highest_class,
        contractUrl: item.contract_url,
        deliveredByUserId: item.delivered_by_user_id,
        deliveredByUserName: item.delivered_by_user_name,
        adcAuthorizedAt: item.adc_authorized_at || null,
        adcAuthorizedById: item.adc_authorized_by || null,
        deliveryMode: item.delivery_mode || 'self',
        beneficiaryId: item.beneficiary_id || null,
        beneficiaryName: item.beneficiary_name || null,
        receivedByUserId: item.received_by_user_id || null,
        receivedByUserName: item.received_by_user_name || null
    }),
    purchase_requests: (item: any): PurchaseRequest => ({
        id: item.id,
        internalCode: item.internal_code || undefined,
        materialName: item.material_name,
        quantity: item.quantity,
        originalQuantity: item.original_quantity,
        unit: item.unit,
        justification: item.justification,
        supervisorId: item.supervisor_id,
        status: item.status,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        receivedAt: item.received_at ? new Date(item.received_at) : null,
        category: item.category,
        area: item.area,
        contractId: item.contract_id,
        contractName: item.contract_name,
        lotId: item.lot_id,
        notes: item.notes,
        approverId: item.approver_id,
        approvalDate: item.approval_date ? new Date(item.approval_date) : null,
        requesterName: item.requester_name,
        approverName: item.approver_name,
        tenantId: item.tenant_id,
        purchaseOrderId: item.purchase_order_id,
        rejectionReason: item.rejection_reason,
        rejectionDate: item.rejection_date ? new Date(item.rejection_date) : undefined,
        adcAuthorizedAt: item.adc_authorized_at || null,
        adcAuthorizedById: item.adc_authorized_by || null,
        batchId: item.batch_id || null,
        requestTarget: item.request_target || 'supplier',
        clientId: item.client_id || null,
        clientName: item.client_name || null,
        sentToClientAt: item.sent_to_client_at || null,
        sentToClientEmail: item.sent_to_client_email || null,
    }),
    return_requests: (item: any): ReturnRequest => ({
        id: item.id,
        internalCode: item.internal_code,
        supervisorId: item.supervisor_id,
        supervisorName: item.supervisor_name,
        materialId: item.material_id,
        materialName: item.material_name,
        quantity: item.quantity,
        unit: item.unit,
        status: item.status,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        completionDate: item.completion_date ? new Date(item.completion_date) : undefined,
        notes: item.notes,
        handlerId: item.handler_id,
        handlerName: item.handler_name,
        tenantId: item.tenant_id,
        returnCondition: item.return_condition,
        evidenceUrl: item.evidence_url,
        contractId: item.contract_id || null,
        contractName: item.contract_name || null,
    }),
    purchase_lots: (item: any): PurchaseLot => ({
        id: item.id,
        name: item.name,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        creatorId: item.creator_id,
        creatorName: item.creator_name,
        status: item.status,
        supplierId: item.supplier_id,
    }),
    purchase_orders: (item: any): PurchaseOrder => ({
        id: item.id,
        supplierId: item.supplier_id,
        supplierName: item.supplier_name,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        creatorId: item.creator_id,
        creatorName: item.creator_name,
        status: item.status,
        requestIds: item.request_ids || [],
        items: item.items || [],
        lotId: item.lot_id,
        pdfUrl: item.pdf_url,
        officialOCId: item.official_oc_id,
        processedAt: item.processed_at ? new Date(item.processed_at) : undefined,
        processedBy: item.processed_by,
        totalAmount: item.total_amount,
        costCenterId: item.cost_center_id || null,
        tenantId: item.tenant_id,
    }),
    cost_centers: (item: any): CostCenter => ({
        id: item.id,
        code: item.code,
        name: item.name || '',
        type: item.type || 'Operaciones',
        budget: Number(item.budget) || 0,
        status: item.status || 'active',
        responsibleId: item.responsible_id || undefined,
        responsibleName: item.responsible_name || undefined,
        startDate: item.start_date || undefined,
        endDate: item.end_date || undefined,
        notes: item.notes || undefined,
        createdBy: item.created_by || undefined,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        updatedAt: item.updated_at ? new Date(item.updated_at) : undefined,
        tenantId: item.tenant_id,
    }),
    quote_requests: (item: any): QuoteRequest => ({
        id: item.id,
        internalCode: item.internal_code,
        title: item.title || '',
        status: item.status,
        requestIds: item.request_ids || [],
        items: item.items || [],
        supplierIds: item.supplier_ids || [],
        responses: item.responses || [],
        deadline: item.deadline || undefined,
        notes: item.notes || undefined,
        awardedSupplierId: item.awarded_supplier_id || undefined,
        awardedQuoteId: item.awarded_quote_id || undefined,
        awardedAt: item.awarded_at || undefined,
        purchaseOrderId: item.purchase_order_id || undefined,
        createdBy: item.created_by || undefined,
        createdByName: item.created_by_name || '',
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        updatedAt: item.updated_at ? new Date(item.updated_at) : undefined,
        tenantId: item.tenant_id,
    }),
    goods_receipts: (item: any): GoodsReceipt => ({
        id: item.id,
        internalCode: item.internal_code,
        purchaseOrderId: item.purchase_order_id,
        purchaseOrderCode: item.purchase_order_code || '',
        supplierId: item.supplier_id || undefined,
        supplierName: item.supplier_name || '',
        items: item.items || [],
        photos: item.photos || [],
        notes: item.notes || undefined,
        receivedBy: item.received_by || undefined,
        receivedByName: item.received_by_name || '',
        receivedAt: item.received_at || new Date().toISOString(),
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        tenantId: item.tenant_id,
    }),
    suppliers: (item: any): Supplier => ({
        id: item.id,
        name: item.name,
        categories: item.categories || [],
        rut: item.rut || undefined,
        bank: item.bank || undefined,
        accountType: item.account_type || undefined,
        accountNumber: item.account_number || undefined,
        email: item.email || undefined,
        address: item.address || undefined,
        phone: item.phone || undefined,
        contacts: item.contacts || [],
        documents: item.documents || [],
        evaluations: item.evaluations || [],
        costCenterId: item.cost_center_id || undefined,
        notes: item.notes || undefined,
    }),
    supplier_payments: (item: any): SupplierPayment => ({
        id: item.id,
        supplierId: item.supplier_id,
        invoiceNumber: item.invoice_number,
        amount: item.amount,
        issueDate: item.issue_date ? new Date(item.issue_date) : new Date(),
        dueDate: item.due_date ? new Date(item.due_date) : new Date(),
        status: item.status,
        createdAt: item.created_at ? new Date(item.created_at) : undefined,
        purchaseOrderNumber: item.purchase_order_number,
        purchaseOrderId: item.purchase_order_id || null,
        work: item.work,
        paymentDate: item.payment_date || item.paid_date ? new Date(item.payment_date || item.paid_date) : undefined,
        paymentMethod: item.payment_method,
        pdfURL: item.pdf_url,
    }),
    salary_advances: (item: any): SalaryAdvance => ({
        id: item.id,
        workerId: item.worker_id,
        workerName: item.worker_name,
        amount: item.amount,
        requestedAt: item.requested_at ? new Date(item.requested_at) : new Date(),
        status: item.status,
        processedAt: item.processed_at ? new Date(item.processed_at) : undefined,
        approverId: item.approver_id,
        approverName: item.approver_name,
        rejectionReason: item.rejection_reason,
        tenantId: item.tenant_id,
    }),
    assigned_checklists: (item: any): AssignedSafetyTask => ({
        id: item.id,
        templateId: item.template_id,
        templateTitle: item.template_title,
        supervisorId: item.supervisor_id,
        assignerId: item.assigner_id,
        assignerName: item.assigner_name,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        status: item.status,
        area: item.area,
        items: item.items,
        observations: item.observations,
        evidencePhotos: item.evidence_photos,
        performedBy: item.performed_by,
        completedAt: item.completed_at ? new Date(item.completed_at) : undefined,
        reviewedBy: item.reviewed_by,
        rejectionNotes: item.rejection_notes,
    }),
    checklist_templates: (item: any): ChecklistTemplate => ({
        id: item.id,
        title: item.title,
        items: item.items || [],
        createdBy: item.created_by,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
    }),
    safety_inspections: (item: any): SafetyInspection => ({
        id: item.id,
        inspectorId: item.inspector_id,
        inspectorName: item.inspector_name,
        inspectorRole: item.inspector_role,
        date: item.date ? new Date(item.date) : new Date(),
        area: item.area ?? item.work,
        location: item.location,
        description: item.description,
        riskLevel: item.risk_level ?? item.risklevel,
        actionPlan: item.action_plan ?? item.actionplan,
        evidencePhotoUrl: item.evidence_photo_url ?? item.evidencephotourl,
        evidencePhotos: item.evidence_photos ?? item.evidencephotos,
        assignedTo: item.assigned_to ?? item.assignedto,
        deadline: item.deadline ? new Date(item.deadline) : undefined,
        status: item.status,
        completionNotes: item.completion_notes ?? item.completionnotes,
        completionExecutor: item.completion_executor,
        completionPhotos: item.completion_photos ?? item.completionphotos,
        completedAt: item.completed_at ? new Date(item.completed_at) : undefined,
        completionSignature: item.completion_signature ?? item.completionsignature,
        reviewedBy: item.reviewed_by,
        rejectionNotes: item.rejection_notes ?? item.rejectionnotes,
    }),
    behavior_observations: (item: any): BehaviorObservation => ({
        id: item.id,
        obra: item.obra,
        workerId: item.worker_id ?? item.workerid,
        workerName: item.worker_name ?? item.workername,
        workerRut: item.worker_rut ?? item.workerrut,
        observationDate: item.observation_date ? new Date(item.observation_date) : (item.observationdate ? new Date(item.observationdate) : new Date()),
        items: item.items || [],
        riskLevel: item.risk_level ?? item.risklevel,
        feedback: item.feedback,
        observerSignature: item.observer_signature ?? item.observersignature,
        workerSignature: item.worker_signature ?? item.workersignature,
        observerId: item.observer_id,
        observerName: item.observer_name,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        evidencePhoto: item.evidence_photo ?? item.evidencephoto,
    }),
    work_items: (item: any): WorkItem => ({
        id: item.id,
        tenantId: item.tenant_id,
        projectId: item.project_id,
        contractId: item.contract_id || null,
        name: item.name,
        type: item.type,
        status: item.status,
        parentId: item.parent_id,
        path: item.path,
        progress: item.progress || 0,
        plannedStartDate: item.planned_start_date ? new Date(item.planned_start_date) : null,
        plannedEndDate: item.planned_end_date ? new Date(item.planned_end_date) : null,
        actualStartDate: item.actual_start_date ? new Date(item.actual_start_date) : null,
        actualEndDate: item.actual_end_date ? new Date(item.actual_end_date) : null,
        unit: item.unit,
        quantity: item.quantity,
        unitPrice: item.unit_price,
        assignedTo: item.assigned_to,
        createdBy: item.created_by,
        rejectionReason: item.rejection_reason ?? null,
    }),
    progress_logs: (item: any): ProgressLog => ({
        id: item.id,
        tenantId: item.tenant_id,
        workItemId: item.work_item_id,
        date: item.date,
        quantity: item.quantity,
        userId: item.user_id,
        userName: item.user_name,
        observations: item.observations,
        photoUrl: item.evidence_urls?.[0] || item.photo_url,
    }),
    payment_states: (item: any): PaymentState => ({
        id: item.id,
        internalCode: item.internal_code || null,
        contractorId: item.contractor_id,
        contractorName: item.contractor_name,
        createdAt: item.created_at,
        totalValue: item.total_value ?? item.total_amount ?? 0,
        earnedValue: item.earned_value ?? 0,
        previousEarned: item.previous_earned ?? 0,
        periodEarned: item.period_earned ?? 0,
        status: item.status,
        items: item.items || [],
        workItemRootId: item.work_item_root_id || null,
        contractId: item.contract_id || null,
        contractName: item.contract_name || null,
        approvedAt: item.approved_at || null,
        approvedByName: item.approved_by_name || null,
        paidAt: item.paid_at || null,
        annulledAt: item.annulled_at || null,
        notes: item.notes || null,
        tenantId: item.tenant_id,
    }),
    daily_talks: (item: any): DailyTalk => ({
        id: item.id,
        tenantId: item.tenant_id,
        obra: item.obra,
        fecha: item.fecha,
        expositorId: item.expositor_id,
        expositorName: item.expositor_name,
        temas: item.temas,
        asistentes: item.asistentes || [],
        firma: item.firma,
        foto: item.foto,
        createdAt: item.created_at,
    }),
    maintenance_orders: (item: any): MaintenanceOrder => ({
        id: item.id,
        internalCode: item.internal_code,
        tenantId: item.tenant_id,
        materialId: item.material_id,
        materialName: item.material_name,
        type: item.type,
        status: item.status,
        priority: item.priority,
        createdAt: item.created_at,
        scheduledDate: item.scheduled_date || null,
        completedAt: item.completed_at || null,
        reportedBy: item.reported_by,
        assignedTo: item.assigned_to,
        assignedToName: item.assigned_to_name,
        description: item.description,
        rootCauseAnalysis: item.root_cause_analysis,
        preventiveAction: item.preventive_action,
        partsUsed: item.parts_used || [],
        downtimeHours: item.downtime_hours,
        totalCost: item.total_cost,
    }),
    maintenance_logs: (item: any): MaintenanceLog => ({
        id: item.id,
        tenantId: item.tenant_id,
        materialId: item.material_id,
        orderId: item.order_id,
        timestamp: item.timestamp,
        action: item.action,
        performedBy: item.performed_by,
        performedByName: item.performed_by_name,
        type: item.type,
    }),
    ea_documents: (item: any): EADocument => ({
        id: item.id,
        tenantId: item.tenant_id,
        employeeId: item.employee_id,
        employeeName: item.employee_name,
        filePath: item.file_path || null,
        fileUrl: item.file_url || null,
        status: item.status,
        generatedAt: item.generated_at,
        confirmedAt: item.confirmed_at || null,
        generatedBy: item.generated_by,
    }),
    protocol_templates: (item: any): ProtocolTemplate => ({
        id: item.id,
        tenantId: item.tenant_id,
        title: item.title,
        type: item.type,
        activityType: item.activity_type ?? '',
        objective: item.objective ?? '',
        normativa: item.normativa ?? [],
        responsibilities: item.responsibilities ?? [],
        items: item.items ?? [],
        createdBy: item.created_by,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
    }),
    protocols: (item: any): Protocol => ({
        id: item.id,
        tenantId: item.tenant_id,
        templateId: item.template_id ?? null,
        workItemId: item.work_item_id ?? null,
        title: item.title,
        type: item.type,
        activityType: item.activity_type ?? '',
        obra: item.obra ?? '',
        objective: item.objective ?? '',
        normativa: item.normativa ?? [],
        responsibilities: item.responsibilities ?? [],
        items: item.items ?? [],
        status: item.status,
        evidencePhotos: item.evidence_photos ?? [],
        executorSignature: item.executor_signature ?? null,
        supervisorSignature: item.supervisor_signature ?? null,
        qualityManagerSignature: item.quality_manager_signature ?? null,
        rejectionReason: item.rejection_reason ?? null,
        createdBy: item.created_by,
        createdAt: item.created_at ? new Date(item.created_at) : new Date(),
        completedAt: item.completed_at ? new Date(item.completed_at) : null,
        reviewedAt: item.reviewed_at ? new Date(item.reviewed_at) : null,
    }),
};
