// Ruta espejo para el módulo RRHH: misma página que Asistencia → Contratos
// (fuente única). RRHH tiene contracts:manage y gestiona la dotación; el detalle
// de cada contrato sigue viviendo en /dashboard/attendance/contracts/[id].
export { default } from '@/app/dashboard/attendance/contracts/page';
